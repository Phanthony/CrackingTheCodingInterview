"""
Stack Min:
How would you design a stack which, 
in addition to push and pop, 
has a function min which returns the minimum element?
"""

"""
Have a class that does everything a regular stack already does,
but also keeps track of the min element when adding in new elements.
"""
